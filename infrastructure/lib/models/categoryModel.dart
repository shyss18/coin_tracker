import 'package:domain/entities/category.dart';

class CategoryModel extends Category {
  const CategoryModel({String color, String title})
      : super(categoryColor: color, categoryTitle: title);
}
